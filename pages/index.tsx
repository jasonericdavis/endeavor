import Layout from '../components/layout';

export default () => {
    return (
        <Layout>This is the dashboard</Layout>            
    )
}

{/* <Link as={`/p/${props.title}`} href={`/post?title=${props.title}`}></Link> */}